name: CI Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu:latest
    steps:
      - uses: actions/checkout@v3
      - name: hello world
        run: echo "Welcome to te testing"
        shell: bash

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo "Goodbye World"
        shell: bash
